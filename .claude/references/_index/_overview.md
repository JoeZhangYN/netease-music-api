<!-- auto-generated by migrate scan --crosscut overview at unix=1777511568 | DO NOT EDIT -->

# 横切索引总览

> 15 主题快速导航；详细请打开各 `_index/<topic>.md`

- **errors** (未生成) — 跑 `migrate scan --crosscut=errors` 创建
- **io-domain** (未生成) — 跑 `migrate scan --crosscut=io-domain` 创建
- **io-application** (未生成) — 跑 `migrate scan --crosscut=io-application` 创建
- **io-adapters** (未生成) — 跑 `migrate scan --crosscut=io-adapters` 创建
- **ports** (未生成) — 跑 `migrate scan --crosscut=ports` 创建
- **ports-impls** (未生成) — 跑 `migrate scan --crosscut=ports-impls` 创建
- **use-cases** (未生成) — 跑 `migrate scan --crosscut=use-cases` 创建
- **domain-invariants** (未生成) — 跑 `migrate scan --crosscut=domain-invariants` 创建
- **commands** (未生成) — 跑 `migrate scan --crosscut=commands` 创建
- **middlewares** (未生成) — 跑 `migrate scan --crosscut=middlewares` 创建
- **pipelines** (未生成) — 跑 `migrate scan --crosscut=pipelines` 创建
- **spawn-points** (未生成) — 跑 `migrate scan --crosscut=spawn-points` 创建
- **shared-state** (未生成) — 跑 `migrate scan --crosscut=shared-state` 创建
- **channels** (未生成) — 跑 `migrate scan --crosscut=channels` 创建
- **domain-sot** (12 项) — 例: `crates/adapter/src/web/handler/admin.rs:180`
